# 设计：补充过程输出物样例资产

## 设计目标

让学习者不仅知道输出物名称，还能直接看到输出物的典型形态。

## 关键决策

### 使用 `输出物/` 目录承载样例

样例文件和章节正文不应混在同一级，因此在章节目录下单独建立 `输出物/`，用于保存：

- 输出物索引
- 文件型输出物样例
- 必要的说明性文件

### 优先覆盖文件型输出物

不是所有输出都是天然文件，例如“工作绩效数据”更偏数据集合；因此本次优先为最常见、最适合模板化理解的文件型输出物提供样例。

### 保持样例可阅读、可复用

样例使用 Markdown 编写，便于直接阅读、修改和后续继续扩展。
